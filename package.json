{
  "name": "good-eats",
  "version": "1.0.0",
  "private": true,
  "description": "Good Eats — full-stack recipe app (React client + Express/MongoDB API), deployed as a single web service.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/src/index.js"
  }
}
